# IMPOSSIBILITY-CHARTER

The paper's target is negative: current FFT-style lower-bound methods cannot improve on this substrate by passing from multiplicative complexity to additive complexity. The mult/add conversion is treated as a map between cost manifolds. On each side it is coherent; across the bounded/unbounded coefficient boundary it loses the descent gradient. A stronger FFT-style lower bound would need a continuous route through that boundary. The substrate witnesses say no such route is available.

The witness chain is: `ROTATIONAL-SUBSTRATE` for the kinematic arena; `ISOPERIMETRIC-EXCHANGE` for non-interchangeable currencies; `POLYGONAL-LEDGER` for constants and cyclotomic value data; `VERTEXIOUS-ALGEBRA` for unbounded cyclotomic depth against affine flatness; `INTERIOR-RESIDUUM` for admissibility and L-W safety. `LANDFALL-EXPORT` supplies the proof-template precedent: finite closure fails exactly where the missing correction lives.

Earned already: the cited witnesses, `NATIVE-F-MINIMAL-DEFINITION`, the forced `K_n` closure choice, and the K-H-L-A proxy context. Still to earn in the paper: the formal impossibility statement, the precise cost-manifold map, and the proof that the witness chain blocks FFT-style descent rather than merely suggesting it.

## Abstract

## CCS

# Paper

TODO:OUTLINE
