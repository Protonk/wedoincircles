# ROTATIONAL-SUBSTRATE

The `rotations/` register sits underneath the program's three named registers — algebraic closure, Sobolev, discrepancy — as substrate of operational machinery and direct technical input. Nine integrations dispersed this supply across `memos/`, `BNHA/`, `fft/`, and `iso/`.

## Operational supply

`rotations/3DT-BRIEF.md` §"Burrows–Wheeler and perfectly clustering Lyndon words" records the Ferenczi–Zamboni identification: every three-distance encoding word is a perfectly clustering Lyndon word. This is candidate machinery for the Kraft-cyclotomic bridge stated at `BNHA/VILLAINS-ANSWERED.md` §#5 — finite-word, Kraft-shaped, separable from the geometric circle. The construction from Lyndon avatar to a prefix-free ledger coupled to `V_cert` is open. The candidate is logged at `fft/FOUR-FRAMEWORK-SYNTHESIS.md` §"Source Inputs To The Construction" and `fft/PROVENANCE-AND-TRANSFERABILITY.md` §3.1 as candidate input to the fifth-coordinate construction agenda.

The Lefèvre–Muller–Tisserand compressed-orbit loop on `(gamma, delta, d, u, v)` with Euclidean updates is the 1-dimensional algorithmic template for the empirical-to-density proxy at `memos/KRAFT-BUDGET-ONE-DIMENSIONAL.md` Step 5. It handles the rotation orbit without enumerating points. Beck 1994's higher-dimensional Fourier + second-moment + Borel–Cantelli machinery is the adjacent-register precedent.

Two-sided primitives — natively on log and circle sides without a functorial bridge — have named instances at `BNHA/triad/PLUS_ULTRA.md` §"Two-sided primitive pattern": 3DT (rotation orbit / machine arithmetic), `K_n` (algebraic / geometric), strip bridge (candidate). The `K_n` instance carries an algebraic-side proof at `fft/HEIDEMAN-JOHNSON-BURRUS-1985-BRIEF.md` §5: `K_n` is the unique multiplicatively-closed half of the involution decomposition `Q(ζ_n) = K_n ⊕ K_n · 2i sin(2π/n)` under `σ_{−1}`. The class is candidate-shaped.

## Direct technical input

The Avila–Jitomirskaya exponential-rate Diophantine parameter is `β(α) = limsup ln q_{n+1}(α) / q_n(α)`. Finite irrationality-measure results force `β(π) = 0`; K-H-L-A's effective-`C` lives downstream of this classification.

`rotations/CONTINUED-FRACTIONS-CROSSWALK.md` indexes six independent perspectives on continued-fraction convergents: spectral (10-martinis), combinatorial (Berthé–Reutenauer), algorithmic (Lefèvre–Muller), computational (Gosper), probabilistic (Beck 1994), effective-transcendence (K-H-L-A).

`rotations/3DT-BRIEF.md` §"Program-Facing Consequences", item 2 records that the three rotation papers do not require Stern–Brocot, Farey, Thomae, or Minkowski `?`. This BIND-compatibility serves as worked precedent for `memos/OLD-TIME-RELIGION.md`'s content-not-calendar criterion and the `iso/THREE-REGISTER-SYNTHESIS.md` Beck audit.

## Enabled and open

Enabled. K-H-L-A's discrepancy branch acquires a 1-dim algorithmic template (Lefèvre–Muller) under a fixed Diophantine classification of `π` (`β(π) = 0`). The Gentle Criminal §#5 bet acquires a candidate Kraft-cyclotomic machinery (Ferenczi–Zamboni Lyndon). The CF substrate is indexed at six perspectives. Fourier-discrepancy BIND-audit has a worked precedent.

Open. Whether the Lyndon/BWT avatar promotes to a prefix-free ledger whose costs track cyclotomic depth rather than word structure. Whether the empirical-to-density proxy closes at the K-H-L-A budget level. Whether the two-sided primitive class extends beyond the three named instances. The program's three open struts (compute-cost, Kraft–Parseval discrepancy, K-H-L-A endgame) are not closed by this dispersal.
